apply plugin: 'eclipse'
apply plugin: 'idea'

allprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        compile "org.jetbrains:annotations:+"
    }

    version = 2.0
}
